[project]
name = "swatch-fixture"
version = "0.1.0"
description = "Deterministic fixture project for SWP-1 tests"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
